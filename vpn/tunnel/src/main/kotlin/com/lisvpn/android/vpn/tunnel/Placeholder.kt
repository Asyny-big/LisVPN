package com.lisvpn.android.vpn.tunnel

/**
 * Reserved namespace for advanced tunnel orchestration:
 *  - Network change reactor (Wi-Fi <-> LTE)
 *  - Multi-tunnel scenarios (split tunnel by SSID)
 *  - DNS hijack policy holder
 *
 * Implementation lands in Phase 5+. Empty file keeps Gradle sync happy.
 */
internal const val MODULE_TAG: String = "vpn-tunnel"
